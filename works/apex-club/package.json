{
  "name": "apex-club",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "start": "python3 -m http.server 8080"
  },
  "engines": {
    "node": ">=18"
  }
}
